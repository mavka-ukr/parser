import ASTNode from "./ASTNode.js";

/**
 * перервати
 */
class BreakNode extends ASTNode {
    /**
     * @param context
     */
    constructor(context) {
        super(context);
    }
}

export default BreakNode;
